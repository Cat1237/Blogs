import XCTest
@testable import LearnFromSourceKIt

final class LearnFromSourceKItTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(LearnFromSourceKIt().text, "Hello, World!")
    }
}
